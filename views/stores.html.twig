<!DOCTYPE html>
<html>

  <head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <meta charset="utf-8">
    <title>MANY STORES to MANY BRANDS</title>
  </head>

  <body>
    <div class="jumbotron">

      <h3>list of all stores</h3>

      {% if stores is not empty %}

        {% for store in stores %}
          <a href="/store/{{store.getId}}"><h3>{{store.getName}}</h3></a>
        {% endfor %}

      {% endif %}

      <h4>add new store: </h4>

      <form action="/stores" method="post">

        <label for="name">name of store:</label>
        <input id="name" type="text" name="store_name">
        <button type="submit" class="btn btn-sm btn-success">save store</button>

      </form>
</html>
