<!DOCTYPE html>
<html>

  <head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css?family=Baloo+Bhaina" rel="stylesheet">
    <meta charset="utf-8">
    <title>MANY STORES to MANY BRANDS</title>
  </head>

  <body>

    <div class="jumbotron">

      <a href="/"><button type="button" class="btn btn-xs btn-default">return</button></a>

      <h4>stores</h4>

      <a href="/create_store"><button class="btn btn-xs btn-default">add store</button></a>
      <a href="/clear_stores"><button class="btn btn-xs btn-danger">remove all stores</button></a>

      {% for store in stores %}

        <p><a href="/store/{{store.getId}}">{{store.getName}}</a>

        <form action="/store/{{store.getId}}/delete" method="post">
          <input name="_method" type="hidden" value="delete">
          <button type="submit">remove store</button>
        </form></p>

      {% endfor %}


      <h4>brands</h4>
      <hr>
      <a href="/create_brand"><button class="btn btn-xs btn-default">add brand</button></a>
      <a href="/clear_brands"><button class="btn btn-xs btn-danger">remove all brands</button></a>
      <br><br>

      {% for brand in brands %}
      <p><a href="/brand/{{brand.getId}}">{{brand.getName}}</a></p>
      {% endfor %}

    </body>
  </html>
